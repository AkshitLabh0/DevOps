apiVersion: v1
kind: Service
metadata:
  name: happy-paws-blue
  namespace: happy-paws-bg
  labels:
    app: happy-paws
spec:
  selector:
    app: happy-paws
    track: blue
  ports:
    - name: http
      port: 80
      targetPort: 80
  type: ClusterIP
