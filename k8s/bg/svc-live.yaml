apiVersion: v1
kind: Service
metadata:
  name: happy-paws-svc
  namespace: happy-paws-bg
  labels:
    app: happy-paws
spec:
  selector:
    app: happy-paws
    track: blue   # blue starts as the “live” version
  ports:
    - name: http
      port: 80
      targetPort: 80
  type: NodePort
